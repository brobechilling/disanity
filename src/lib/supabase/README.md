# Supabase Client

Place Supabase browser-client setup here when `@supabase/supabase-js` is installed.

Suggested files later:

- `client.ts` for the typed Supabase browser client
- `database.types.ts` generated from the Supabase schema

Keep this folder focused on low-level Supabase configuration. Query logic should live in `src/repositories`.
